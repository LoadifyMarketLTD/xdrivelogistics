[build]
  command = "npm run build:all"
  # Removed: publish = "dist" - conflicts with @netlify/plugin-nextjs
  # Plugin requires exclusive deployment control

[[plugins]]
  package = "@netlify/plugin-nextjs"
