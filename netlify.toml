[build]
  command = "npm run build:all"
  
[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js plugin - handles deployment automatically
# Landing page integrated into Next.js public/ directory
[[plugins]]
  package = "@netlify/plugin-nextjs"
