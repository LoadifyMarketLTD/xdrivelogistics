[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect - ensures client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
